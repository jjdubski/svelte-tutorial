# TODO List

- Update templates to have better titles and descriptions
  - Also use some bold, italics and a url (in at least one of them) for templates to introduce users to those features
  - Include a list for one template using: -
  - Change one of the templates to use the Ideas category
- Update StatsBar loading to not jitter when switching routes
- Mobile calender squares turn into rectangles when a task is inside of it
  - Change the layout for mobile calender to have a similar layout to apple calender
  - Use lines with the tag colors to show there is a task for that day (grey default)
- Add category doesn't work (Remove)
- Ensure svelte5 syntax and best practices are used throughout the codebase
- Make sure there is test coverage for localStorage (and switching from localStorage to authenticated flow)
- Add animations to buttons where they are missing
- Come up with new ideas/features
